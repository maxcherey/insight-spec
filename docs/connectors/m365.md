# Microsoft 365 Connector Specification

> Version 1.0 — March 2026
> Based on: `docs/CONNECTORS_REFERENCE.md` Source 6 (Microsoft 365)

Standalone specification for the Microsoft 365 (Communication) connector. Expands Source 6 in the main Connector Reference with complete table schemas, identity mapping, Silver/Gold pipeline notes, and open questions.

<!-- toc -->

- [Overview](#overview)
- [Bronze Tables](#bronze-tables)
  - [`ms365_email_activity` — Email activity per user per date](#ms365emailactivity-email-activity-per-user-per-date)
  - [`ms365_teams_activity` — Teams activity per user per date](#ms365teamsactivity-teams-activity-per-user-per-date)
  - [`ms365_onedrive_activity` — OneDrive activity per user per date](#ms365onedriveactivity-onedrive-activity-per-user-per-date)
  - [`ms365_sharepoint_activity` — SharePoint activity per user per date](#ms365sharepointactivity-sharepoint-activity-per-user-per-date)
  - [`ms365_copilot_usage` — Copilot AI usage per user per date](#ms365copilotusage-copilot-ai-usage-per-user-per-date)
  - [`ms365_collection_runs` — Connector execution log](#ms365collectionruns-connector-execution-log)
- [Identity Resolution](#identity-resolution)
- [Silver / Gold Mappings](#silver-gold-mappings)
- [Open Questions](#open-questions)
  - [OQ-M365-1: Copilot endpoint availability and schema](#oq-m365-1-copilot-endpoint-availability-and-schema)
  - [OQ-M365-2: OneDrive and SharePoint Silver mapping](#oq-m365-2-onedrive-and-sharepoint-silver-mapping)

<!-- /toc -->

---

## Overview

**API**: Microsoft Graph API v1.0 — Report endpoints (`/reports/get*ActivityUserDetail`)

**Category**: Communication

**Identity**: `userPrincipalName` (UPN, email address) — resolved to canonical `person_id` via Identity Manager

**Field naming**: camelCase — original Graph API field names, no renaming at Bronze level.

**Why six tables**: The Graph API exposes Email, Teams, OneDrive, SharePoint, and Copilot activity as separate endpoints with different report periods and field sets. Each is stored as its own Bronze table sharing the same identity key (`userPrincipalName + reportRefreshDate`). The collection run log is a seventh monitoring-only table.

> **Critical: Data Retention Window**
> M365 Graph API returns only the **last 7–30 days** of activity. Data cannot be re-fetched once the window passes — loss is permanent. The collector must run at minimum every 7 days to avoid data gaps.

---

## Bronze Tables

### `ms365_email_activity` — Email activity per user per date

| Field | Type | Description |
|-------|------|-------------|
| `unique` | text | Primary key |
| `userPrincipalName` | text | User email (UPN) — identity key |
| `reportRefreshDate` | date | Report date |
| `reportPeriod` | text | Report period duration, e.g. `"7"` |
| `displayName` | text | User display name |
| `isDeleted` | boolean | Whether the account is deleted |
| `assignedProducts` | jsonb | M365 licenses assigned |
| `sendCount` | numeric | Emails sent |
| `receiveCount` | numeric | Emails received |
| `readCount` | numeric | Emails read |
| `meetingCreatedCount` | numeric | Meetings created via email |
| `meetingInteractedCount` | numeric | Meeting interactions via email |
| `lastActivityDate` | date | Last email activity (NULL if none) |

---

### `ms365_teams_activity` — Teams activity per user per date

| Field | Type | Description |
|-------|------|-------------|
| `unique` | text | Primary key |
| `userPrincipalName` | text | User email (UPN) — identity key |
| `userId` | text | Microsoft internal user ID |
| `reportRefreshDate` | date | Report date |
| `reportPeriod` | text | Report period duration |
| `tenantDisplayName` | text | M365 tenant name |
| `isDeleted` | boolean | Whether the account is deleted |
| `isLicensed` | boolean | Whether user has Teams license |
| `isExternal` | boolean | Whether user is an external guest |
| `hasOtherAction` | boolean | Whether user had other Teams actions not captured by specific metrics |
| `assignedProducts` | jsonb | M365 licenses assigned |
| `teamChatMessageCount` | numeric | Messages in team/group chats |
| `privateChatMessageCount` | numeric | Messages in private (1:1) chats |
| `postMessages` | numeric | Posts published in team channels |
| `replyMessages` | numeric | Replies to channel posts |
| `callCount` | numeric | Calls made |
| `meetingCount` | numeric | Total meetings count |
| `meetingsAttendedCount` | numeric | Meetings attended |
| `meetingsOrganizedCount` | numeric | Meetings organized |
| `adHocMeetingsAttendedCount` | numeric | Ad-hoc (unscheduled) meetings attended |
| `adHocMeetingsOrganizedCount` | numeric | Ad-hoc meetings organized |
| `scheduledOneTimeMeetingsAttendedCount` | numeric | One-time scheduled meetings attended |
| `scheduledOneTimeMeetingsOrganizedCount` | numeric | One-time scheduled meetings organized |
| `scheduledRecurringMeetingsAttendedCount` | numeric | Recurring meetings attended |
| `scheduledRecurringMeetingsOrganizedCount` | numeric | Recurring meetings organized |
| `audioDuration` | text | Total audio call duration |
| `videoDuration` | text | Total video duration |
| `screenShareDuration` | text | Total screen sharing duration |
| `urgentMessages` | numeric | Messages sent with urgent priority |
| `sharedChannelTenantDisplayNames` | text | Tenant names for shared channels |
| `lastActivityDate` | date | Last Teams activity |

**Total chat messages** = `teamChatMessageCount + privateChatMessageCount`. `postMessages` and `replyMessages` are channel activity (content publishing), not direct messaging — exclude from message counts.

---

### `ms365_onedrive_activity` — OneDrive activity per user per date

| Field | Type | Description |
|-------|------|-------------|
| `unique` | text | Primary key |
| `userPrincipalName` | text | User email (UPN) — identity key |
| `reportRefreshDate` | date | Report date |
| `reportPeriod` | text | Report period duration |
| `isDeleted` | boolean | Whether the account is deleted |
| `assignedProducts` | jsonb | M365 licenses assigned |
| `viewedOrEditedFileCount` | numeric | Files viewed or edited |
| `syncedFileCount` | numeric | Files synced via desktop client |
| `sharedInternallyFileCount` | numeric | Files shared with internal users |
| `sharedExternallyFileCount` | numeric | Files shared externally |
| `lastActivityDate` | date | Last OneDrive activity |

---

### `ms365_sharepoint_activity` — SharePoint activity per user per date

| Field | Type | Description |
|-------|------|-------------|
| `unique` | text | Primary key |
| `userPrincipalName` | text | User email (UPN) — identity key |
| `reportRefreshDate` | date | Report date |
| `reportPeriod` | text | Report period duration |
| `isDeleted` | boolean | Whether the account is deleted |
| `assignedProducts` | jsonb | M365 licenses assigned |
| `viewedOrEditedFileCount` | numeric | SharePoint files viewed or edited |
| `visitedPageCount` | numeric | SharePoint pages visited |
| `syncedFileCount` | numeric | Files synced from SharePoint |
| `sharedInternallyFileCount` | numeric | Files shared internally |
| `sharedExternallyFileCount` | numeric | Files shared externally |
| `lastActivityDate` | date | Last SharePoint activity |

---

### `ms365_copilot_usage` — Copilot AI usage per user per date

**Source endpoint**: `getMicrosoft365CopilotUsageUserDetail` — per-user daily AI usage across all Copilot-enabled Office apps.

> ⚠️ Schema proposed from Graph API naming conventions — verify field names against the live endpoint before implementation.

| Field | Type | Description |
|-------|------|-------------|
| `unique` | text | Primary key |
| `userPrincipalName` | text | User email (UPN) — identity key |
| `reportRefreshDate` | date | Report date |
| `reportPeriod` | text | Report period duration |
| `displayName` | text | User display name |
| `isDeleted` | boolean | Whether the account is deleted |
| `assignedProducts` | jsonb | M365 licenses assigned |
| `lastActivityDate` | date | Most recent Copilot activity across all apps (NULL if none) |
| `copilotChatLastActivityDate` | date | Last Microsoft 365 Copilot Chat activity |
| `teamsLastActivityDate` | date | Last Teams Copilot activity |
| `wordLastActivityDate` | date | Last Word Copilot activity |
| `excelLastActivityDate` | date | Last Excel Copilot activity |
| `powerPointLastActivityDate` | date | Last PowerPoint Copilot activity |
| `oneNoteLastActivityDate` | date | Last OneNote Copilot activity |
| `outlookLastActivityDate` | date | Last Outlook Copilot activity |
| `loopLastActivityDate` | date | Last Loop Copilot activity |
| `teamsActionCount` | numeric | Teams Copilot AI actions taken |
| `wordActionCount` | numeric | Word Copilot AI actions taken |
| `excelActionCount` | numeric | Excel Copilot AI actions taken |
| `powerPointActionCount` | numeric | PowerPoint Copilot AI actions taken |
| `oneNoteActionCount` | numeric | OneNote Copilot AI actions taken |
| `outlookActionCount` | numeric | Outlook Copilot AI actions taken |
| `loopActionCount` | numeric | Loop Copilot AI actions taken |

**Total Copilot actions** = sum of all `*ActionCount` fields. Per-app `lastActivityDate` fields are NULL for apps where the user has had no Copilot activity in the report window.

---

### `ms365_collection_runs` — Connector execution log

| Field | Type | Description |
|-------|------|-------------|
| `run_id` | text | Unique run identifier |
| `started_at` / `completed_at` | timestamp | Run timing |
| `status` | text | `running` / `completed` / `failed` |
| `email_records_collected` | numeric | Rows collected for `ms365_email_activity` |
| `teams_records_collected` | numeric | Rows collected for `ms365_teams_activity` |
| `onedrive_records_collected` | numeric | Rows collected for `ms365_onedrive_activity` |
| `sharepoint_records_collected` | numeric | Rows collected for `ms365_sharepoint_activity` |
| `copilot_records_collected` | numeric | Rows collected for `ms365_copilot_usage` (0 until endpoint enabled) |
| `api_calls` | numeric | API calls made |
| `errors` | numeric | Errors encountered |
| `settings` | jsonb | Collection configuration (report period, enabled endpoints) |

Monitoring table — not an analytics source.

---

## Identity Resolution

`userPrincipalName` (UPN, email address) is the primary identity key across all M365 Bronze tables.

The Identity Manager resolves `userPrincipalName` → canonical `person_id` in Silver step 2. The M365 UPN is typically the corporate email address, making it a reliable cross-system identity anchor.

`ms365_teams_activity` also contains `userId` (Microsoft internal user ID) — this is not used for cross-system resolution; `userPrincipalName` takes precedence.

---

## Silver / Gold Mappings

| Bronze table | Silver target | Status |
|-------------|--------------|--------|
| `ms365_email_activity` | `class_communication_events` | ✓ Mapped |
| `ms365_teams_activity` | `class_communication_events` | ✓ Mapped |
| `ms365_onedrive_activity` | *(unmapped)* | Available — no unified stream defined yet |
| `ms365_sharepoint_activity` | *(unmapped)* | Available — no unified stream defined yet |
| `ms365_copilot_usage` | *(unmapped)* | Planned — will likely feed a `class_ai_usage` stream alongside Cursor, Windsurf, Copilot |

**Gold**: `class_communication_events` feeds communication-level Gold metrics (message counts, meeting counts, collaboration intensity). No Gold tables are defined specifically for M365 file storage or Copilot activity yet.

---

## Open Questions

### OQ-M365-1: Copilot endpoint availability and schema

The `getMicrosoft365CopilotUsageUserDetail` endpoint requires a Microsoft 365 Copilot license on the tenant. Field names in `ms365_copilot_usage` are proposed based on Graph API patterns — the live endpoint must be queried to confirm the exact response shape before implementation.

### OQ-M365-2: OneDrive and SharePoint Silver mapping

`ms365_onedrive_activity` and `ms365_sharepoint_activity` are collected but not yet unified. If a `class_file_storage` or similar Silver stream is defined, both tables feed it. Decision pending on whether file-storage metrics are in scope for Silver.
